# Instalación

#### Entrar al proyecto

```bash
cd nombre-del-repo
```



#### Revisar archivos clave

Busca si existen:

- `docker-compose.yml` (configuración de contenedor)
- `Dockerfile`
- `.env.example`